# Radar — 2026-08-21 (execução agendada)

> Segunda entrada sob a regra de 19/ago: prosa do agente humanizada antes do commit; citação verbatim,
> número, data, link e ⏳ intocados.

## Nota de ambiente

GitHub inteiro. Os papers do arXiv saíram da fila diária ontem, depois de oito rotas bloqueadas, e
hoje não foram tentados — a decisão está registrada na entrada de 20/ago e não se repete aqui.

A varredura de corpus tinha precedência declarada sobre leitura nova, e foi cumprida: OpenClaw,
Hermes, n8n e o repositório do Grok Build, todos na primária.

## Consultas (5 WebFetch de primária)

[`openclaw/openclaw` releases](https://github.com/openclaw/openclaw/releases) ✓ ·
[`NousResearch/hermes-agent` releases](https://github.com/NousResearch/hermes-agent/releases) ✓ ·
[`hermes-agent` tag `v2026.8.18`](https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.18) ✓ ·
[`n8n-io/n8n` releases](https://github.com/n8n-io/n8n/releases) ✓ ·
[`xai-org/grok-build`](https://github.com/xai-org/grok-build) ✓

**Leituras no repositório do livro**: `livro/apendice-estudo.md`, `radar/RADAR.md`,
`radar/diario/2026-08-20.md`, `radar/diario/2026-08-17.md`.

## Achados

### 1. ⭐⭐ Um quinto desenho de procedência de plugin, e o primeiro que **inspeciona**

[Hermes v0.20.4](https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.18) (18/ago),
verbatim:

> *"**NVIDIA SkillEvaluator Tier 1 advisory scanning on skill installs (license + security checks)**"*

Três palavras dessa linha carregam o achado.

**"Scanning"** — nenhum dos quatro desenhos registrados em 17/ago olha para dentro do artefato. O
CompozyOS exige digest, que prova identidade e nada diz sobre conteúdo. O OpenClaw exige um gesto
explícito. O Claude Code bloqueia por lista. O IronClaw instala por link. Este abre e examina.

**"License + security"** — a verificação tem dois eixos, e o primeiro é o que o holaOS expôs em
13/ago, quando um repositório que se anuncia *open-source* trazia licença com restrição de uso
comercial. Um instalador que checa licença automaticamente responde a esse problema no ponto de
entrada.

**"Advisory"** — e aqui está o limite. O scanner **avisa e não impede**. Some-se o "Tier 1", que
sugere níveis, e o desenho fica claro: inspeção graduada, decisão do usuário. É o mesmo lugar da curva
onde o OpenClaw parou, com informação melhor para quem decide.

O espectro do apêndice de supply chain vai a cinco entradas, e ganha um eixo que ele não tinha. Os
quatro primeiros diferem em **quanto atrito** impõem; este difere em **o que se sabe** antes de
decidir. **Impacto B**, somando-se ao **B→A** já registrado para o apêndice.

Vale registrar quem faz a verificação: um terceiro (NVIDIA), não o fornecedor do harness nem o
registro de skills. A cadeia de confiança ganha mais um elo, e o cap. **12** não discute quem audita
o auditor.

### 2. ⭐ Hermes migrou para MCP 2.x com suporte a *stateless*

Da página de releases, [v0.20.3](https://github.com/NousResearch/hermes-agent/releases) (16/ago), na
lista de novidades: *"MCP 2.x SDK migration with stateless protocol support"*.

Seria a primeira adoção de campo do núcleo *stateless* da spec **2026-07-28** por um membro do corpus.
O cap. **06** foi revisado na spec 060 justamente porque a Leitura executiva celebrava o que a spec
nova depreciou, e uma migração real muda o argumento de "a spec mudou" para "os harnesses estão indo".

⏳ **Não confirmei esta linha na página da release individual.** Ela vem do resumo da lista de
releases, e a regra da casa é que resumo não vira citação. Fica como achado provável, marcado, com a
leitura do `v2026.8.16.2` no topo da fila. **C** até a confirmação, **B** depois dela.

### 3. ⭐ A divergência de licença do Grok Build tem explicação, e o nosso apêndice está certo

Ontem registrei que o `agent.json` do registro do ACP declara `"license": "proprietary"` para o Grok
Build, enquanto o nosso apêndice o traz como Apache-2.0. Fui ao repositório.

[`xai-org/grok-build`](https://github.com/xai-org/grok-build), verbatim:

> *"First-party code in this repository is licensed under the **Apache License, Version 2.0**"*

E a distribuição descrita ali é de **binários pré-compilados** para macOS, Linux e Windows, de um CLI
em Rust. O registro do ACP, por sua vez, aponta para um pacote npm: `@xai-official/grok@1.0.7`.

São artefatos diferentes com nomes diferentes. O repositório de código é aberto; o pacote npm que o
registro instala é declarado proprietário por quem submeteu a entrada. **O teste de inclusão do cap.
01 §6 pergunta pelo que é inspecionável na data de corte, e isso é o repositório** — que segue Apache
2.0, como o apêndice diz. Nenhuma correção necessária.

⏳ **O que fica em aberto é a identidade**: `@xai-official/grok` e `xai-org/grok-build` são o mesmo
produto empacotado de duas formas, ou coisas distintas? Não resolvi, e não vou resolver por inferência
de nome — foi exatamente esse atalho que produziu os três erros de ontem.

### 4. n8n: credencial resolvida por usuário final

[Releases](https://github.com/n8n-io/n8n/releases) de 20 e 21/ago, em `2.35.6`, `2.36.4` e `1.123.75`:
*"End-user credentials resolution for node parameters"* e *"Chat and MCP trigger authentication
validation for end-user credentials"*.

Num harness embutido, a credencial que o agente usa pode ser a do dono do fluxo ou a de quem disparou
a execução, e a diferença decide quem responde pelo que a ferramenta fez. O cap. **15** trata o
arquétipo embutido pela forma da tarefa e pelo provedor do modelo, sem tocar em identidade de
execução. **C**, com nota de que o tema aparece agora em dois pontos independentes do mesmo release.

### 5. OpenClaw: sem release novo, e um item de 15/ago que eu não tinha registrado

Nenhuma release entre 16 e 21/ago; a mais recente segue a `2026.8.1-beta.2`, já no Radar desde 17/ago.
Relendo suas notas, um item passou batido: *"Trusted-proxy browser pairing: optional auto-approval for
Control UI devices from allowlisted proxy identities with non-admin scope caps."*

Aprovação automática decidida pela **identidade do proxy**, com teto de escopo. É a terceira variante
do mesmo mecanismo em uma semana: o CompozyOS usa procedência para **restringir**, o n8n usa para
**dispensar** aprovação, e aqui ela **aprova sozinha, mas com limite de alcance**. A terceira é a mais
interessante das três, porque combina as duas metades. Acrescentado à linha existente do `RADAR.md`,
sem item novo.

## Descartes

| Alegação | Motivo do descarte |
|---|---|
| *"bundled Bot Mode plugin with **teammate protocol**"* — Hermes v0.20.3 | Nome de protocolo novo, vindo de **resumo de lista de releases**, sem página individual lida e sem especificação localizada. Não entra nem como ⏳ de protocolo: entra como pergunta na fila. Se existir mesmo, é assunto do cap. 17 |
| Métricas de PR das releases do Hermes (~74 e ~125 PRs) | São o que a release **afirma**, não medição minha. Registro a existência das releases e suas datas; não uso os números |
| arXiv 2603.00195, 2601.06007, 2606.25189, 2605.21392 | **Fora da fila diária desde ontem**, por oito rotas bloqueadas. Pendência do editor. Não tentados hoje, e isso é decisão, não esquecimento |
| `devblogs.microsoft.com` sobre Agent Harness no BUILD 2026 | **Quarto dia sem leitura.** A varredura de corpus tinha precedência declarada e consumiu o orçamento. Continua na fila, e vai perdendo valor: se não couber amanhã, sai |
| Frameworks (LangGraph, CrewAI, OpenAI Agents SDK, Software Agent SDK) e IronClaw | **Não conferidos hoje.** Foram lidos em 18/ago; a dívida é de três dias e volta ao topo amanhã |

## Fila para amanhã

1. **Confirmar a migração MCP 2.x do Hermes** na página do `v2026.8.16.2`. É o achado 2, e ele muda de
   **C** para **B** com uma citação verbatim.
2. **Os quatro frameworks e o IronClaw** — três dias sem primária.
3. **O "teammate protocol" do Hermes** — existe? Onde está descrito?
4. **`devblogs.microsoft.com`**, quarta e última tentativa de agenda.
5. **Correção de ficha pendente desde 17/ago**: `benchmark/avaliacoes/hermes-agent.md` não menciona
   A2A, embarcado desde 03/ago. Item **B**, fora do meu alcance de escrita.
