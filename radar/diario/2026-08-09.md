# Radar — 2026-08-09 (execução agendada)

## Nota de ambiente (afeta a verificação)

O proxy de egresso desta execução **bloqueou** `arxiv.org` (terceiro dia seguido), `zed.dev`,
`agentclientprotocol.com`, `openreview.net`, `huggingface.co`, `picrew.github.io` e
`agentic-ai.readthedocs.io`. Em compensação, **o GitHub passou inteiro** — e foi lá que esta
execução trabalhou: **nove páginas de repositório e de release lidas como primária**.

Isto cumpre o compromisso deixado ontem. A execução de 08/ago terminou com um eixo vazio porque a
busca genérica por *"release notes"* só devolveu agregadores, e ficou escrito que "a próxima
execução deve ir direto às páginas de release dos repositórios". Foi o que se fez, e o rendimento
inverteu: **quatro dos cinco achados de hoje saíram de notas de release lidas na fonte**, nenhum de
notícia.

## Consultas (4 WebSearch + 9 WebFetch de verificação)

1. "Agent Client Protocol ACP Zed adoption 2026 opencode goose editors agents specification"
2. "agent harness paper preprint August 2026 sandbox escape context compaction evaluation new results"
3. "'Agent Harness Engineering: A Survey' ETCLOVG authors TMLR affiliation seven layers"
4. "new coding agent harness launched week August 2026 open source github release announcement CLI"

**Fetches de verificação primária** (releases e repositórios, todos lidos nesta execução):
[`anomalyco/opencode/releases`](https://github.com/anomalyco/opencode/releases) ·
[`openai/codex/releases`](https://github.com/openai/codex/releases) ·
[`openai/codex` tag `rust-v0.147.0`](https://github.com/openai/codex/releases/tag/rust-v0.147.0) (notas literais) ·
[`block/goose/releases`](https://github.com/block/goose/releases) ·
[`All-Hands-AI/OpenHands/releases`](https://github.com/All-Hands-AI/OpenHands/releases) ·
[`zed-industries/agent-client-protocol`](https://github.com/zed-industries/agent-client-protocol) ·
[`badlogic/pi-mono`](https://github.com/badlogic/pi-mono) ·
[changelog do Claude Code](https://code.claude.com/docs/en/changelog) ·
tentativas bloqueadas em `zed.dev`, `agentclientprotocol.com`, `huggingface.co`,
`agentic-ai.readthedocs.io`.

## Achados

### 1. ⭐⭐ A camada de protocolo se mexeu **dentro do corpus**, em três frentes e em três velocidades
Este é o achado do dia, e ele só aparece quando se lê release em vez de notícia — nenhuma das três
frentes virou manchete.

**MCP — o corpus começa a implementar a spec nova.** O [Codex `rust-v0.147.0`](https://github.com/openai/codex/releases/tag/rust-v0.147.0)
(07/ago), literal: *"Support the opt-in MCP 2026-07-28 protocol, including paginated discovery,
multi-round requests, and non-blocking server startup"*, com *"Upgrade the MCP SDK to 3.0.0"*. A
spec **2026-07-28** entrou no Radar em 01/ago com a ação sugerida "dados de adoção para a matriz
(rodada 2026-10)" — este é o **primeiro membro do corpus** que se pode registrar implementando-a, e
implementando **por opt-in**, o que já diz algo sobre a confiança na transição.

**ACP — adoção silenciosa, dentro dos harnesses.** Dois membros do corpus mexeram em código ACP em
onze dias, e nenhum dos dois anunciou:

- [opencode v1.18.14](https://github.com/anomalyco/opencode/releases) (05/ago): *"Waited for queued
  ACP session updates before ending a turn"*
- [goose v1.45.0](https://github.com/block/goose/releases) (29/jul): *"Gate tool-call label
  enrichment on ACP client capability"*

Primária do protocolo, lida hoje: [`zed-industries/agent-client-protocol`](https://github.com/zed-industries/agent-client-protocol)
— **Apache-2.0**, **3,9 mil estrelas / 336 forks** (11,6:1 ✓), **não arquivado**, descrição corrente
*"A protocol for connecting any editor to any agent"*, e o README declarando que ele *"standardizes
communication between code editors … and coding agents"*, com bibliotecas oficiais em **Kotlin,
Java, Python, Rust e TypeScript**. ⏳ **a contagem de adotantes não foi verificada**: `zed.dev` e
`agentclientprotocol.com` estão bloqueados, e os números que circulam (25+ agentes, JetBrains desde
dez/2025, registro público em jan/2026) vieram de agregador. O que **está** verificado é melhor que
uma contagem: **código ACP em duas notas de release de membros do corpus**.

**A2A — 150+ organizações no papel e, no corpus, um fornecedor preferindo canal próprio.** É o
achado de ontem visto de outro ângulo: o Claude Code, na mesma janela, construiu
[mensageria entre sessões](https://code.claude.com/docs/en/changelog) (`SendMessage`/`ListAgents`,
v2.1.224) em vez de adotar protocolo.

**A leitura que isto habilita, e que o cap. 17 hoje não faz: três protocolos, três camadas, três
velocidades.** MCP (agente↔ferramenta) tem spec nova e implementação começando. ACP
(editor↔agente) tem adoção **silenciosa** — entra por dentro, sem anúncio, porque resolve um
problema que o usuário sente (usar o agente no editor que já tem). A2A (agente↔agente) tem o maior
número de organizações declaradas e a menor evidência de uso no corpus, e o fornecedor mais maduro
preferiu canal proprietário. **A ordem de adoção não segue a ordem de anúncio — segue a de dor.**

**Impacto B** para os caps. **17** e **06**, e dado direto para a matriz de adoção da rodada 2026-10.

### 2. ⭐ Codex 0.147.0: a aprovação **delegada a um revisor automático** — e segredos vazando pelo histórico
Fonte primária: [notas literais da tag `rust-v0.147.0`](https://github.com/openai/codex/releases/tag/rust-v0.147.0),
publicada em **07/ago**. Quatro linhas interessam ao cap. 07, e uma delas ao cap. 08:

- *"Enable automatically reviewed approvals with the new `--approve-for-me` CLI flag."* — o portão
  de aprovação, que o cap. 07 apresenta como **o** ponto em que o humano entra no laço, passa a
  poder ser operado por um revisor automático. Não é o `--full-auto` de antes (ver abaixo): é uma
  revisão, feita por máquina, no lugar da humana. Casa com o que o Claude Code fez na v2.1.225
  (*"the model is now told to move on rather than retry"* depois de uma recusa do filtro de
  segurança **sobre a própria checagem de permissão**): dois harnesses, na mesma semana, movendo
  julgamento de aprovação para dentro do sistema.
- *"Remove the deprecated `codex exec --full-auto` flag; use `--sandbox workspace-write` instead."*
  — e este é o movimento oposto, no mesmo release. O modo "tudo liberado" **morre**, substituído por
  um **nível de sandbox nomeado**. Os dois juntos contam a história certa: não se está afrouxando a
  contenção, está-se **trocando o botão binário por gradação** e automatizando o julgamento dentro
  da gradação.
- *"Harden plugin isolation and deny network access when policy updates fail."* — **fail-closed**
  explícito: falhou a atualização de política, corta a rede. É o exemplo citável que o cap. 07
  pede quando argumenta que a postura padrão em falha define a contenção.
- *"Redact secrets and complete bearer tokens from displayed commands and replayed conversation
  history."* — **segredos e bearer tokens completos** apareciam em comandos exibidos e no
  **histórico reproduzido**. O vazamento não estava no runtime: estava na **memória**. O cap. 08
  trata persistência como problema de continuidade e custo; aqui ela é superfície de exposição —
  o segredo que passou uma vez pelo terminal fica guardado e **volta a ser exibido** a cada replay.

Uma quinta linha vai para outro capítulo: *"Import Cursor-managed skills and synchronize changes to
imported Claude and Cursor conversations without creating duplicates."* — interoperabilidade **por
artefato**, não por protocolo: um harness lendo as *skills* e as *conversas* de dois concorrentes.
Enquanto ACP e A2A padronizam o **fio**, o Codex padronizou o **arquivo do outro**. Caps. **12** e
**14**.

**Impacto B** (07, 08); **C** para 12/14.

### 3. ⭐ opencode: a **compactação repetida** perdia resultado de ferramenta
Fonte primária: [releases do opencode](https://github.com/anomalyco/opencode/releases), **v1.18.15
(07/ago)**, literal: *"Repeated compaction now keeps earlier tool-call history in summaries instead
of dropping orphaned results"*. E, no mesmo release: *"Chronological message ordering now stays
correct even when imported or legacy message IDs are out of order"*.

O cap. 04 trata compactação como **técnica** — o que é, quando disparar, o que preservar. O que
falta lá é o que este release entrega de graça: um **modo de falha de produção, datado e nomeado**.
Não é "compactar perde contexto", que é óbvio e genérico. É específico: quando a compactação
acontece **mais de uma vez**, o resumo da segunda passada pode deixar para trás resultados de
ferramenta cuja chamada correspondente já saiu do histórico — **resultados órfãos**. O agente segue
com um resumo que menciona uma ação e não tem o que ela devolveu.

É a compactação como **operação repetida** que quebra, não como operação única — e nenhum capítulo
do livro discute a compactação da compactação. Combina com o que a spec 082 já registrou no
Prime Agent (histórico completo, inclusive compactações passadas, acessível por programa) e com o
que a v2.1.225 do Claude Code corrigiu na mesma semana (*"Fixed conversation history breaking on
Remote Control session resume after very large conversations were compacted"*).

**Impacto B** para o cap. **04**: a seção pode ganhar um trecho sobre modos de falha, com três casos
datados e independentes.

### 4. OpenHands: conversa-filha vira **ação tipada**
Fonte primária: [releases do OpenHands](https://github.com/All-Hands-AI/OpenHands/releases).
**v1.11.0 (07/ago)**: *"add a typed agent action for launching local or Cloud child conversations"*
e *"show per-run LLM cost in the Activity Log and exports"*. **v1.10.0 (05/ago)**: *"set Canvas
default model to GLM 5.2"*, *"activity log export"*, *"filter the skills page with a faceted rail"*.

Dois pontos, os dois **C**: subagente deixa de ser convenção de prompt e vira **ação tipada** do
protocolo interno do agente, com destino local ou em nuvem (cap. 10, dimensão 10); e o **GLM 5.2**
— o modelo da Z.ai, cujo harness ZCode foi descartado do corpus em 02/ago **por ser fechado** —
passa a ser o **padrão** de um recurso de um membro do corpus. Modelo aberto atravessa a fronteira
que o harness fechado não atravessa: é a mesma tese do padrão "modelo aberto, harness fechado",
vista do lado de quem consome. Nota para o apêndice de cadeia de suprimentos.

### 5. goose: **sem release na janela** — e isso é dado
Último release é o [v1.45.0, de 29/jul](https://github.com/block/goose/releases). Onze dias sem
publicar, num projeto que é **âncora de fundação** (AAIF/Linux Foundation, registrado em 06/ago).
Não é sinal de dormência — a cadência histórica do goose não foi medida nesta execução e **não se
afirma nada sobre ela**. Fica registrado como observação a confrontar no refresh do Apêndice A: um
projeto sob governança de fundação tem cadência diferente de um projeto de fornecedor? O corpus
agora tem os dois tipos e pode responder com dado.

## Descartes

### "O Pi cruzou **54 mil estrelas**" — o agregador está defasado em ~37%, para baixo
A alegação aparece em [The Tool Nerd](https://www.thetoolnerd.com/p/10-agent-harnesses-every-ai-builder).
O Pi é **membro do corpus** (spec 064), então o contrato manda passar pelo repositório.

[Primária](https://github.com/badlogic/pi-mono), lida hoje: **85,8 mil estrelas / 10,7 mil forks**
(8,0:1 ✓), **MIT**, TypeScript, **não arquivado**, **5.582 commits**, 24 PRs e 69 issues abertos,
descrição corrente *"AI agent toolkit: unified LLM API, agent loop, TUI, coding agent CLI"*.

**Descartado por defasagem** — e o caso vale registro porque **fecha um catálogo**. Em três dias o
Radar viu os três modos de erro de agregador, e eles apontam para direções diferentes:

| Modo | Caso | Direção do erro |
|---|---|---|
| **Inflação** | Claw Code, 06/ago — 194.982★ para 109.281 forks vendidos como "framework com 72k estrelas" | exagera o que existe |
| **Obituário** | gemini-cli, 07 e 08/ago — projeto vivo declarado encerrado desde junho | apaga o que existe |
| **Defasagem** | Pi, hoje — 54k contra 85,8k reais | congela o que existe |

A defasagem é a **menos escandalosa e por isso a mais copiável**: não soa errado, soa apenas velho,
e ninguém confere um número que parece plausível. Se o livro citasse 54k, não estaria mentindo —
estaria desatualizado em quatro dígitos, num apêndice cuja função é justamente ser atual.

### O survey **ETCLOVG**: ⏳ mantida, dúvida afiada, e um aviso sobre o próprio instrumento
Ontem o item entrou como possível duplicata do survey de Meng et al. Hoje tentei resolver por
quatro caminhos, e **os quatro estão bloqueados**: `openreview.net` (fórum e PDF), `huggingface.co`
(o dataset `ChenLiu1996/Agent-Harness-Engineering`), `picrew.github.io` (a página do projeto) e
`agentic-ai.readthedocs.io`.

O que a busca de hoje acrescenta, **e não é primária**: as afiliações citadas para o ETCLOVG são um
consórcio acadêmico norte-americano (Carnegie Mellon, Yale, Stanford, Northeastern, Tulane, UAB,
UChicago, Virginia Tech, Rutgers, Amazon), o que **inclina** para ser trabalho distinto do de Meng
et al. — cujos 11 autores estão verificados na
[primária](https://github.com/Gloriaameng/Awesome-Agent-Harness).

**Mas o resumo da busca também atribuiu ao ETCLOVG exatamente "110+ papers, 23 systems" — os mesmos
números de Meng et al.** Isso pode ser coincidência reveladora *ou* o resumidor misturando dois
resultados da mesma página de busca (o repositório do Meng et al. era o primeiro resultado). É
exatamente a falha que 08/ago registrou com o changelog. **Portanto esse número não é usado como
evidência em direção nenhuma** — nem para afirmar duplicata, nem para negá-la. Segue **⏳**, com o
que falta nomeado: ler a autoria em `openreview.net/forum?id=3hXEPbG0dh`.

### Outros
- **Claude Code**: nenhuma versão nova em 09/ago. A mais recente é a **v2.1.226 (08/ago)**, cujo
  conteúdo integral é *"Bug fixes and reliability improvements"*. A v2.1.225, do mesmo dia, já foi
  registrada ontem. Nada a acrescentar.
- **Harness novo**: nenhum na janela. Muse Code (05/ago), QM (31/jul) e Pi já estão no Radar; as
  listagens que os apresentam como novidade são de catálogo, não de lançamento. Apareceu um
  **segundo diretório curado** da categoria em dois dias
  ([`bradagi/awesome-cli-coding-agents`](https://github.com/bradagi/awesome-cli-coding-agents),
  depois do `best-of-Agent-Harnesses` de ontem) — anotado como sinal de maturação da categoria, sem
  triagem nesta execução.
- **Papers**: `arxiv.org` bloqueado pelo **terceiro dia seguido**. O contraditório da auto-evolução
  (arXiv **2607.12227**) segue no Radar com a ⏳ aberta em 07/ago. A busca por papers novos devolveu
  só material já registrado (Code as Agent Harness, ClawVM, Lingering Authority) e um repositório
  companheiro novo para um deles
  ([`YennNing/Awesome-Code-as-Agent-Harness-Papers`](https://github.com/YennNing/Awesome-Code-as-Agent-Harness-Papers)) — **C**, sem item próprio.
- **Kimi Code e Grok Build**: não consultados nesta execução, por orçamento. Ficam no topo da fila
  da próxima.

## Leituras executivas em risco

Nenhuma invalidada. **Uma qualificada, três reforçadas:**

- **Cap. 04 (Compactação)** — *qualificada hoje*: a leitura trata compactação como resposta à
  janela de contexto e não registra **modos de falha**. Há agora três casos datados e independentes
  na mesma semana — resultados órfãos na compactação repetida (opencode v1.18.15), histórico
  quebrando em resume depois de compactação (Claude Code v2.1.225) e a subordinação da compactação
  ao agente (Prime Agent, spec 082). A síntese se mantém; falta a seção de como ela falha.
- **Cap. 17 (Protocolos)** — *reforçada*: a qualificação de ontem (interoperabilidade ≠ coordenação)
  ganha hoje evidência de campo em três velocidades, com **código ACP em release de dois membros do
  corpus** como a parte verificada.
- **Cap. 06 (MCP)** — a leitura já estava marcada para revisão (spec 060). O que muda hoje é que
  existe **implementação real** da spec 2026-07-28 num membro do corpus, e por opt-in.
- **Cap. 07 (Permissões e sandbox)** — *reforçada pelo terceiro dia*: aprovação revisada
  automaticamente e remoção do `--full-auto` no mesmo release, isolamento de plugin **fail-closed**,
  e um vazamento de bearer token que morava no **histórico**, não no runtime.

## Observação de método

O compromisso de ontem se pagou no mesmo dia, e o número é claro: **ir direto às páginas de release
rendeu quatro achados; a busca por notícia, zero.** Vale escrever a regra que isso sugere, para o
editor decidir se entra no contrato: *para os sistemas do corpus, a varredura começa nas páginas de
release dos repositórios, e a busca por notícia serve para o que está **fora** do corpus.* Notícia é
boa para descobrir o que não se sabia existir; para acompanhar quem já se conhece, ela chega tarde,
resumida e — como o caso do Pi mostrou hoje — às vezes defasada em quatro dígitos.

E o catálogo de falhas de agregador fechou em três: inflação, obituário, defasagem. O `AGENTE.md`
descreve os dois primeiros. A **defasagem** merece o mesmo parágrafo, com a nota de que ela é a mais
perigosa das três justamente por não parecer erro.
