# Radar — 2026-08-23 (execução agendada)

> Quarta entrada sob a regra de 19/ago: prosa humanizada antes do commit; citação verbatim, número,
> data, link e ⏳ intocados.

## Nota de ambiente

**O clone do repositório tinha sumido** — container reciclado. O contrato prevê o caso e manda clonar
de novo antes de qualquer coisa; foi o que fiz, e a `main` voltou em `46f5c6c`.

**E o instrumento de leitura falhou de um jeito específico, que vale registrar.** Duas páginas de
**lista de releases** (LangGraph e OpenAI Agents SDK) devolveram datas de **2024**, o que contradiz
leituras minhas de 11 e 16/ago nos mesmos repositórios. Testei a hipótese numa página de **tag**, cujo
conteúdo eu já conhecia: veio correta, com data e links de issue.

Então a regra do dia passou a ser: **lista de releases não serve como fonte hoje; página de tag serve.**
Refiz por tag o que dava para refazer, e o que não deu ficou sem afirmação.

## Consultas (5 WebFetch)

[`crewAIInc/crewAI` releases](https://github.com/crewAIInc/crewAI/releases) ✓ ·
[`langchain-ai/langgraph` releases](https://github.com/langchain-ai/langgraph/releases) ✗ (data implausível) ·
[`openai/openai-agents-python` releases](https://github.com/openai/openai-agents-python/releases) ✗ (data implausível) ·
[`openai-agents-python` tag `v0.22.0`](https://github.com/openai/openai-agents-python/releases/tag/v0.22.0) ✓ ·
[`software-agent-sdk` tag `v1.43.0`](https://github.com/OpenHands/software-agent-sdk/releases/tag/v1.43.0) ✓

**Leituras no repositório do livro**: `radar/RADAR.md`, `radar/diario/2026-08-22.md`,
`radar/diario/2026-08-18.md`.

## Achados

### 1. ⭐⭐ A defesa contra a série de oito falhas está confirmada, e é fase 2b de um programa

Ontem registrei o *AST-backed shell command-name resolution* com ⏳, porque a frase vinha da lista de
novidades. Fui à página da tag.
[v1.43.0](https://github.com/OpenHands/software-agent-sdk/releases/tag/v1.43.0), **21 Aug 09:51**,
verbatim:

> *"feat(security): **AST-backed shell command-name resolution** ([#2721](https://github.com/OpenHands/software-agent-sdk/issues/2721) **Phase 2b**)"*

Duas coisas novas em relação a ontem. A citação existe, com número de issue, o que promove o achado de
⏳ para confirmado. E o **"Phase 2b"** diz que isto não é um conserto avulso: é etapa de um programa de
segurança em fases, num membro do corpus.

O cap. **07** ganha, junto com os oito exemplos da série de falhas de sintaxe, uma defesa **datada,
nomeada e em andamento**. Resolver o nome do comando pela árvore sintática, em vez de casar texto, é o
conserto de categoria para toda a família.

### 2. ⭐⭐ O carregador de Agent Plugins usa **esquema fechado**

Da mesma tag, verbatim:

> *"feat(plugin): add **Agent Plugins manifest loader** (root `plugin.json`, **closed schema**)"*

O detalhe que ontem eu não tinha é *closed schema*. A spec 1.0, lida em 18/ago, define um formato
mínimo e delega ao cliente o que não está nela. Um carregador que valida contra **esquema fechado**
recusa campo desconhecido em vez de ignorá-lo, e isso é uma decisão de segurança: campo extra num
manifesto de plugin é superfície.

Reforça o item de ontem e afina a pergunta da rodada: já sabemos que este cliente **fecha o esquema**;
falta saber o que ele faz com o ambiente do subprocesso, com o `PATH` e com a credencial, que é
exatamente o que a spec deixou em aberto.

### 3. ⭐⭐ Uma guardrail que alcança o estado persistido, e não só o turno

[OpenAI Agents SDK v0.22.0](https://github.com/openai/openai-agents-python/releases/tag/v0.22.0),
**19 Aug 13:44**, verbatim:

> *"**Redacts terminal function-tool output rejected by agent output guardrails from replayable and
> persisted SDK state.**"*

A saída de ferramenta foi recusada pela guardrail, e o conserto **remove esse conteúdo do estado
persistido e do replay**. Sem isso, o que a política barrou no turno continuaria vivo no registro que
o sistema reproduz depois.

É a mesma família do vazamento de *bearer token* pelo **histórico reproduzido** que o Codex corrigiu em
09/ago: em ambos, a defesa cobria o caminho quente e não cobria o durável. O cap. **08** trata memória
e estado pela durabilidade e pela fronteira entre efêmero e persistente, sem perguntar **o que a
política de saída deixa entrar ali**. O cap. **07** trata a política no momento da chamada.

A pergunta que falta aos dois: **uma decisão de recusa precisa alcançar o que sobrevive ao turno?**
Com dois casos datados e independentes, a resposta é sim, e vira seção. **Impacto B.**

Do mesmo release, um vizinho: *"Isolates usage accounting between independent `RunState` checkpoints
while preserving nested-agent aggregation."* Contabilidade de consumo por checkpoint, que conversa com
o *"include compaction usage in run totals"* do release de 16/ago. **C** para o cap. **11**.

### 4. ⭐ A mensagem única que a escada de compactação não resolve

[CrewAI 1.15.17](https://github.com/crewAIInc/crewAI/releases) (20/ago), da lista de novidades:
*"Handle oversized single messages during chunking"*.

A escada do cap. **04** pressupõe que dá para escolher **o que** descartar ou resumir entre várias
mensagens. Uma mensagem que sozinha estoura o orçamento quebra essa premissa: não há o que priorizar,
porque o item indivisível já não cabe.

O capítulo não discute esse caso. É a borda inferior da escada, e ela aparece na prática sempre que
uma tool devolve um blob grande de uma vez. **C**, com nota — a leitura veio de lista de releases, e
hoje lista de releases não é fonte confiável neste ambiente. Confirmar por tag antes de subir de nível.

## Descartes

| Alegação | Motivo do descarte |
|---|---|
| Qualquer afirmação sobre releases do **LangGraph** | A lista devolveu releases de **2024**, contra a minha própria leitura de `1.2.11` em 11/ago. Leitura **inconclusiva**: não afirmo que houve release, nem que não houve |
| As datas que vieram das listas do OpenAI Agents SDK e do CrewAI | Mesmo defeito de instrumento. Para o OpenAI SDK refiz por tag e a data correta é **19 Aug 13:44**; para o CrewAI **não refiz**, então a data de 20/ago fica sem confirmação |
| *"nove contribuidores"* e outras métricas de release | São o que a release **afirma**, não medição minha. Não uso |
| Licença do TrueForge e o que ele chama de *runtime layer* | **Não lido hoje.** O orçamento foi para os frameworks atrasados e para refazer as leituras que o instrumento estragou. Segue na fila |
| `benchmark/avaliacoes/hermes-agent.md` sem A2A | **Não é descarte, é dívida** — pendente desde 17/ago, fora do meu alcance de escrita. Item **B** de correção |

## Fila para amanhã

1. **O que o Software Agent SDK faz com o que a Agent Plugins delega** — ambiente do subprocesso,
   `PATH`, credencial. Já sei que ele fecha o esquema do manifesto; falta o resto, e é leitura de
   código com pergunta pronta desde 22/ago.
2. **Reconferir LangGraph e CrewAI por página de tag**, já que as listas falharam hoje.
3. **TrueForge**: licença na primária e o que o repositório chama de *runtime layer*.
4. **A tese da recusa que alcança o durável** (achado 3) merece uma terceira amostra antes de virar
   seção. Procurar em OpenClaw e Claude Code, que são os que mais mexem em redação de segredo.
