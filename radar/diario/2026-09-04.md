# Radar — 2026-09-04 (execução agendada)

> Décima sexta entrada sob a regra de 19/ago: prosa humanizada antes do commit; citação verbatim,
> número, data, link e ⏳ intocados.

## Nota de ambiente

Censo de avisos de segurança continuado em mais nove membros do corpus. Com os cinco de ontem, são
**catorze checados** e **44 avisos publicados**.

E um deles é notícia do dia, não arquivo: o n8n publicou **dez avisos entre 1º e 3 de setembro**, ou
seja, nas últimas 48 horas.

**Registro de um erro meu, de ontem.** O laço de push que eu venho usando faz
`git push … 2>&1 | tail -2`, e o código de saída de um pipeline é o do último comando. Ontem o push foi
**rejeitado** por não-fast-forward, o `tail` saiu zero, e o laço imprimiu sucesso. É exatamente o
defeito do `tee` da spec 104 deste repositório, reproduzido no meu próprio processo, três semanas depois
de eu tê-lo documentado. Corrigi com `set -o pipefail`, puxei a `main` e o push saiu. A entrada de
ontem estava fechada quando percebi, por isso o registro vem aqui.

## Consultas (9 fontes · 9 requisições web + leituras locais)

Abas `security/advisories` de: [n8n](https://github.com/n8n-io/n8n/security/advisories) ·
[Codex](https://github.com/openai/codex/security/advisories) ·
[OpenHands](https://github.com/All-Hands-AI/OpenHands/security/advisories) ·
[LangGraph](https://github.com/langchain-ai/langgraph/security/advisories) ·
[gemini-cli](https://github.com/google-gemini/gemini-cli/security/advisories) ·
[CrewAI](https://github.com/crewAIInc/crewAI/security/advisories) ·
[OpenAI Agents SDK](https://github.com/openai/openai-agents-python/security/advisories) ·
[IronClaw](https://github.com/nearai/ironclaw/security/advisories) ·
[aider](https://github.com/Aider-AI/aider/security/advisories). Mais leituras locais de `radar/RADAR.md`
e da entrada de 3/set.

Mesmo aviso de método de ontem: **listagens lidas, corpos não**. Título, severidade e data são o
afirmado.

## Achados

### 1. ⭐⭐⭐ O censo: 44 avisos em catorze membros, e a distribuição é o achado

| Sistema | Categoria | Avisos | Janela |
|---|---|---|---|
| [OpenClaw](https://github.com/openclaw/openclaw/security/advisories) | agente pessoal | **10** | 30/jun/2026 |
| [Claude Code](https://github.com/anthropics/claude-code/security/advisories) | harness de código | **10** | fev–jun/2026 |
| [n8n](https://github.com/n8n-io/n8n/security/advisories) | harness embutido | **10** | **1º–3/set/2026** |
| [LangGraph](https://github.com/langchain-ai/langgraph/security/advisories) | framework | **9** | out/2025–ago/2026 |
| [opencode](https://github.com/anomalyco/opencode/security/advisories) | harness de código | **2** | 12/jan/2026 |
| [goose](https://github.com/block/goose/security/advisories) | harness de código | **1** | 24/jul/2026 |
| [Codex](https://github.com/openai/codex/security/advisories) | harness de código | **1** | 19/set/2025 |
| [OpenHands](https://github.com/All-Hands-AI/OpenHands/security/advisories) | harness de código | **1** | 23/mar/2026 |
| gemini-cli · Hermes · IronClaw · aider · CrewAI · OpenAI Agents SDK | várias | **0** | *"There aren't any published security advisories"* |

Quatro sistemas concentram **39 dos 44**. Oito dos catorze publicam **zero**. A distribuição é
bimodal, e isso mede o que eu disse ontem com mais força: ou o projeto tem processo de divulgação
coordenada, ou não tem nada no meio.

Reforça a proposta ao editor, registrada de novo sem edição: a coluna de **postura de divulgação** no
Apêndice A precisa existir justamente porque o número, sozinho, ranqueia ao contrário.

Impacto **A** no cap. 07 (base factual), **B** no Apêndice A.

### 2. ⭐⭐⭐ Os dez do n8n, publicados nas últimas 48 horas

Todos entre 1º e 3/set. Verbatim, os seis que batem em capítulo:

- *"Per-Resource OAuth Consent Bypass via Unbound Refresh Token Resource Substitution"*
  (GHSA-cw9w-vv67-hf73, moderada, 3/set)
- *"Disabled OIDC SSO Endpoints Remain Active and Issue Valid Sessions"* (GHSA-pf83-w3f9-8m37,
  moderada, 2/set)
- *"Agent Workflow Tool Bypasses Sub-Workflow Caller Policy"* (GHSA-7hgx-277f-7vmg, moderada, 2/set)
- *"Domain-Restriction Bypass via Unguarded Model-Search Endpoint in OpenAI Chat Model Node"*
  (GHSA-34ff-336r-5q23, alta, 2/set)
- *"GitHub Trigger 422 Reuse Path Skips Webhook Secret Storage, Causing Signature Verification to
  Fail-Open"* (GHSA-5m98-cgcr-xx3q, moderada, 2/set)
- *"Regular Expression Denial of Service in the Default Blocked-File-Pattern Match via a Git Node Clone
  Path"* (GHSA-j535-v25q-vx3q, alta, 2/set)

Cada um deles fecha um eixo que o Radar vinha abrindo por changelog.

**O token de atualização sem vínculo com o recurso** é a sexta instância do eixo da credencial presa à
origem — e a primeira com aviso publicado. As cinco anteriores eram engenharia defensiva; esta é o
defeito que a defesa previne, documentado.

**O endpoint de SSO desabilitado que continua emitindo sessão válida** é a forma mais grave da família
"desabilitar não desabilitou": os dois do Feishu no OpenClaw (ontem), o `"always allow"` que não
gravava e o modo de agentes desligado que ainda rodava subagentes no gemini-cli. Cinco instâncias, quatro
sistemas — e aqui a desabilitação ignorada **emite credencial de sessão**.

**A ferramenta de fluxo do agente contornando a política de chamador de subfluxo** é a tese de ontem
literalmente, com o agente como o segundo caminho.

**A verificação de assinatura falhando aberta no caminho de reuso do 422** junta duas coisas que eu
tinha separadas: o eixo fail-open/fail-closed e o **caminho de erro como subsistema** (1º/set). O passo
de segurança foi pulado no caminho que trata um código de erro HTTP.

**E o ReDoS no casamento do padrão de arquivos bloqueados** é o mais fino de todos: o mecanismo da
política — a expressão regular que implementa a lista de bloqueio — é ele próprio a vulnerabilidade.

Impacto **B** nos caps. 07, 15 e 12; e o cap. 15, que trata harness embutido, ganha o primeiro corpo de
evidência de segurança que ele tem.

### 3. ⭐⭐ O `git diff` como superfície de injeção, em dois membros independentes

- goose: *"Arbitrary command execution in goose CLI via `goose review` via git core.fsmonitor"*
  (CVE-2026-72718, alta, 24/jul/2026), lido por inteiro em 2/set.
- OpenHands: *"Command Injection in Git Diff Handler"* (GHSA-7h8w-hj9j-8rjw, alta, **23/mar/2026**).

Dois harnesses de código, bases separadas, quatro meses de distância, e o mesmo ponto: o subsistema que
lê o diff do repositório do usuário executa comando. Some a isso os dois avisos de worktree do Claude
Code, o *"Git Node File Sandbox Escape via Relative Remote URL Base-Directory Mismatch"* do n8n (2/set)
e o ReDoS no caminho de clone do nó de git.

**O git é o maior interpretador que o harness invoca, e o harness o invoca sobre entrada do atacante.**
O cap. 07 discute sandbox de comando; falta a observação de que a ferramenta de versionamento é um
motor de execução configurável por arquivo no repositório. Impacto **B**.

### 4. ⭐⭐ A família de vulnerabilidade acompanha a categoria de implantação

Comparando os perfis, os quatro sistemas com volume falham em lugares diferentes.

Nos harnesses de código e no agente pessoal — Claude Code, OpenClaw, opencode, goose, Codex, OpenHands
— o assunto é **caminho, sandbox, aprovação e injeção via git**: symlink, junção de diretório, worktree,
mudança de diretório, wrapper, loopback. O único aviso do Codex diz isso em uma linha: *"Sandbox
bypass due to bug in path configuration logic"* (GHSA-w5fx-fh39-j5rw, alta, 19/set/2025).

No framework LangGraph, nenhum desses aparece. Os nove são **desserialização e injeção em repositório de
estado**: *"Unsafe JSON deserialization in LangGraph checkpoint loading"*, *"Unsafe msgpack
deserialization in LangGraph checkpoint loading"*, *"RCE in 'json' mode of JsonPlusSerializer"*,
*"BaseCache Deserialization of Untrusted Data Remote Code Execution"*, mais duas injeções de SQL em
checkpointer e store, e *"Namespace prefix matching crosses segment boundaries in Postgres and SQLite
stores"*.

Faz sentido: o framework não tem sandbox de shell para escapar, e tem **estado serializado** como
superfície principal. É a conclusão do paper de 1º/set — *"their main differences are driven less by the
foundation model than by deployment priorities"* — aparecendo do lado da segurança.

⏳ **Hipótese, com a amostra declarada.** Entre os frameworks do corpus, só o LangGraph publica avisos;
CrewAI e OpenAI Agents SDK publicam zero. Um perfil de categoria tirado de **um** sistema é hipótese, e
fica assim marcada até haver segundo. Impacto **B** nos caps. 07 e 14 se confirmada.

### 5. ⭐ A política que ignora em silêncio parte de si mesma

LangGraph, alta, 28/ago/2026: *"LangGraph SDK custom auth silently ignores actions= on resource
decorators"*.

O desenvolvedor declarou as ações no decorador, e a camada de autenticação ignorou a declaração. É a
mesma coisa que a regra de permissão gravada que a ferramenta ignorava (26/ago) e que a desabilitação
por conta do Feishu, agora em superfície de framework e com a palavra **silently** no próprio título.

O cap. 07 pergunta o que a política permite. Falta perguntar **o que da política é efetivamente lido**.
Impacto **C**, reforço de um eixo já registrado.

## Descartes

| Alegação | Motivo do descarte |
|---|---|
| Perfil de vulnerabilidade por categoria como conclusão | ⏳ **Hipótese.** Só um framework do corpus publica avisos (LangGraph); CrewAI e OpenAI Agents SDK têm zero. Amostra declarada no achado 4 |
| Qualquer mecanismo dos 43 avisos além do goose | ⏳ **Corpos não lidos**, segundo dia. Título, severidade e data são o afirmado; as famílias vêm da leitura dos títulos |
| Zero aviso como sinal de sistema mais seguro | **Descartado por método**, como ontem. Oito dos catorze publicam zero, e a distribuição bimodal mostra que o número mede processo de divulgação |
| Membros do corpus ainda não checados | **Faltam seis**: Software Agent SDK, OpenHarness, Grok Build, Pi, Kimi Code, QM e Prime Agent. Censo em 14 de 20 |
| Releases do corpus | **Não conferidos hoje.** O censo consumiu o orçamento; última varredura completa em 29/ago, com IronClaw em 31/ago e gemini-cli em 2/set |
| Releases do Hermes Agent | **Não lidos**, sexto dia. Item que mais tempo passa na fila sem sair |
| A Tabela 1 do [arXiv:2605.26112](https://arxiv.org/html/2605.26112v1) célula a célula | **Não lida**, quarto dia na fila |
| `benchmark/avaliacoes/hermes-agent.md` sem A2A e sem MCP 2.x | **Dívida, não descarte** — pendente desde 17/ago, fora do meu alcance de escrita |

## Fila para amanhã

1. **Fechar o censo** nos seis que faltam.
2. **Ler os corpos** dos três que mais interessam ao livro, agora com um quarto candidato: o wrapper do
   `flock` que contorna vínculo durável de aprovação, o loopback de MCP que expõe ferramenta de dono, a
   exfiltração pelo domínio pré-aprovado, e o SSO desabilitado que emite sessão válida.
3. **Releases do corpus**, que ficam seis dias sem varredura completa depois de amanhã — e o **Hermes**,
   que já está em seis.
4. **A Tabela 1 do [arXiv:2605.26112](https://arxiv.org/html/2605.26112v1)** célula a célula.
5. **Fichas com lacuna de protocolo**: `benchmark/avaliacoes/hermes-agent.md` (A2A e MCP 2.x) e a do
   gemini-cli (A2A). Item **B**, fora do meu alcance de escrita.
