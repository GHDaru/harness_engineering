# Tarefas — spec 085

- [x] T01 `smtp_vars` no `/health` (nomes com `repr()`, prefixo `SMTP`)
- [x] T02 teste: nome aparece, valor não
- [x] T03 `EMAIL.md`: como ler a lista
- [x] T04 merge `--no-ff` + push
- [ ] T05 leitura contra produção
